- [ ] Game might not end when its last half inning of the period and the batting teams makes a run to take a lead. Should end because no hope for the other team.
  - **Root cause found (2026-04-10):** `resolve_pending_runs()` in referee.c (lines 798-879) scores runs at 3 sites (lines 818, 832, 858) but has ZERO `endPeriod` checks. Every other scoring path (`update_runs`, `update_free_walk_resolution`) checks and sets `endPeriod` when the period should end. When a pending run resolves, the run is scored but the period-end condition is never evaluated. **Fix planned in Phase 6, Step 6.3.**
- [ ] I witnessed a situation where the AI was in second and third bases and started running from pitch with the second base player but did not start to run with the third base player. So probably unintended and is either a logical error in AI code or action (controller) related bug.
- [x] ~~Free walk does not reset the balls counter when the lead runner accepts the walk (without a batter change).~~ **Fixed (2026-04-12):** `update_free_walk_resolution()` now resets `halfInningState->balls = 0` after processing any accepted free walk.
- [x] ~~AI starts running immediately when a new pitch preparation begins (before the ball is even released) when there are already 2 accumulated balls.~~ **Fixed (2026-04-12):** Caused by bug #3 above — unreset balls triggered immediate re-offering of the free walk.
- [x] ~~Free walk from 3rd base: runner physically ran toward home and could be called OUT.~~ **Fixed (2026-04-12):** Three-part fix: (1) `runToNextBase()` now preserves `ADVANCING_FREELY` state at the flag point instead of overwriting it with `RUNNING`, (2) free walk from 3rd no longer starts a physical run — referee sets `hasScored = 1` and `enforceLegalState` removes the player, (3) `takeFreeWalkDecision` skips `runToNextBase` when base is 3rd.
