- [x] Create `src/game/ai_messy/catching_ai.c` and `.h`. Move `static void moveControlledPlayerToLocation(StateInfo* stateInfo, Vector3D* target)` and `static void throwBallToBase(StateInfo* stateInfo, int base)` from `src/game/action_implementation.c` to `src/game/ai_messy/catching_ai.c`. Also move the static variable `aiMoveCounter` and the `TIMEOUT_CONSTANT` used in `aiLogic`. Ensure `throwing_system.h` is included and functions are exposed in `catching_ai.h` if needed. (commit: 4ee7f1b)
