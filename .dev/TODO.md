- [ ] Create `src/game/actions_messy/action_state.c` and `src/game/actions_messy/action_state.h`. Move `meterCounter` and `meterCounterMax` from `src/game/action_implementation.c` to `action_state.c` (as globals) and expose them in `action_state.h`. Update `src/game/action_implementation.c` to include `actions_messy/action_state.h`. Add `game/actions_messy/action_state.o` to `_OBJ` in `Makefile`.
