- [x] Move batting functions (`selectBatter`, `startIncreaseBatterAngle`, `stopIncreaseBatterAngle`, `startDecreaseBatterAngle`, `stopDecreaseBatterAngle`, `selectPower`, `selectAngle`, `updateBatting`) from `action_implementation.c` to `src/game/actions_messy/batting_system.c`. Ensure they are declared in `.h` and removed from original file. (commit: d213389)
- [x] Extract batting meter logic (cases where `swing` is 1 or 3) from `updateMeters` in `action_implementation.c` to a new function `updateBattingMeter` in `batting_system.c`. Call it from `updateMeters`. (commit: d213389)
- [x] Fix includes in `batting_system.c` (needs `common_logic.h`, `action_state.h`, `math.h`, etc.) and ensure build passes. (commit: d213389)