- [x] Create `src/game/actions_messy/pitching_system.c` and `src/game/actions_messy/pitching_system.h`. Move the functions `startPitch`, `continuePitch`, `releasePitch`, their associated static variable `pitchPower`, and the `#define` constants `PITCH_BASE_SPEED`, `PITCHER_MOVE_AWAY_OFFSET`, `PITCH_POWER_CONSTANT`, `PITCH_ANGLE_CONSTANT`, `PITCH_DOWN_MAX`, `PITCH_UP_MAX`, `VERTICAL_ANGLE_LIMIT` from `src/game/action_implementation.c` to this new module. Update `src/game/action_implementation.c` to include `actions_messy/pitching_system.h` and use the new functions. Add `game/actions_messy/pitching_system.o` to `_OBJ` in `Makefile`. (commit: b3dddaa)