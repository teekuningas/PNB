- [x] Create `src/game/actions_messy/throwing_system.c` and `.h`. Move `genericThrowRelease`, `genericThrowLoad`, `genericMove`, `genericStopMove`, `dropBall`, and `updateControlledPlayerSpeed` from `src/game/action_implementation.c` to the new file. Also move static variables `throwDistance`, `throwDirection` and constants `THROW_TO_BASE_DISTANCE`, `THROW_POWER_CONSTANT`, `THROW_DISTANCE_CONSTANT`, `THROW_MAX`, `DROP_BALL_CONSTANT`. Ensure `genericSlingBall` remains available (or is included properly). (commit: 552e7a5)
